---
phase: 08-domain-separation
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  # Provider 정리 (core/ 이동)
  - flutter_app/lib/core/providers/repository_providers.dart
  # bookmarkProvider (new)
  - flutter_app/lib/presentation/pages/home/providers/bookmark_provider.dart
  # VehicleDetailPage (new)
  - flutter_app/lib/presentation/pages/home/vehicle_detail_page.dart
  # 라우터 업데이트
  - flutter_app/lib/presentation/router/app_router.dart
  - flutter_app/lib/presentation/router/route_names.dart
  # 홈 라우팅 수정
  - flutter_app/lib/presentation/pages/home/home_page.dart
  # _extractQuery 버그 수정
  - flutter_app/lib/presentation/pages/ai_chat/ai_chat_page.dart
  - flutter_app/lib/presentation/pages/ai_chat/chat_history_page.dart
  # card_provider import 정리
  - flutter_app/lib/presentation/pages/ai_card/providers/card_provider.dart
  - flutter_app/lib/presentation/pages/home/providers/home_provider.dart
  - flutter_app/lib/presentation/pages/ai_chat/widgets/inline_card_carousel.dart
autonomous: true
---

<objective>
## Goal
08-01에서 분리한 Repository 인터페이스를 Provider로 연결하고, 홈 라우팅을 VehicleDetailPage로 수정하며, 북마크 토글과 _extractQuery 버그를 수정한다.

## Purpose
08-01이 도메인/데이터 레이어를 분리했으나, Provider와 UI는 아직 이전 구조를 참조. 이 plan에서 presentation 레이어를 새 도메인 구조에 맞게 연결하여 홈→차량상세, 북마크, AI 추천 필터링이 올바르게 동작하도록 한다.

## Output
- `repository_providers.dart` — vehicleRepo/bookmarkRepo/garageRepo 공용 Provider
- `bookmark_provider.dart` — 북마크 상태 관리 Notifier
- `vehicle_detail_page.dart` — 차량 상세 페이지 (북마크 토글 포함)
- 홈 탭 차량 탭 → VehicleDetailPage 라우팅
- _extractQuery 버그 수정 (사용자 입력 기반 쿼리 추출)
- flutter analyze 에러 0
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Prior Work
@.paul/phases/08-domain-separation/08-01-SUMMARY.md

## Source Files
@flutter_app/lib/presentation/pages/ai_card/providers/card_provider.dart
@flutter_app/lib/presentation/pages/home/providers/home_provider.dart
@flutter_app/lib/presentation/pages/home/home_page.dart
@flutter_app/lib/presentation/pages/ai_chat/ai_chat_page.dart
@flutter_app/lib/presentation/pages/ai_chat/widgets/inline_card_carousel.dart
@flutter_app/lib/presentation/router/app_router.dart
@flutter_app/lib/presentation/router/route_names.dart
@flutter_app/lib/domain/repositories/i_bookmark_repository.dart
@flutter_app/lib/data/repositories/go_api/bookmark_repository_impl.dart
</context>

<acceptance_criteria>

## AC-1: Repository Provider 공용화
```gherkin
Given vehicleRepositoryProvider가 3개 이상 feature에서 사용될 때
When core/providers/repository_providers.dart를 확인하면
Then vehicleRepositoryProvider, bookmarkRepositoryProvider, garageRepositoryProvider가 공용 Provider로 정의되어 있다
And card_provider.dart에서 이들을 import하여 사용한다
```

## AC-2: bookmarkProvider 북마크 토글
```gherkin
Given 사용자가 VehicleDetailPage에서 북마크 아이콘을 탭하면
When bookmarkProvider.toggleBookmark(vehicleId)가 호출되면
Then 북마크 상태가 토글되고 (Drift 영속 저장)
And 아이콘이 즉시 채워진/빈 상태로 변경된다
```

## AC-3: VehicleDetailPage + 홈 라우팅
```gherkin
Given 홈 탭에서 차량 카드를 탭하면
When 라우팅이 실행되면
Then /home/vehicle/:id 경로로 VehicleDetailPage가 표시된다 (AI카드 페이지가 아님)
And 차량 스펙, 가격, 북마크 버튼이 표시된다
```

## AC-4: _extractQuery 버그 수정
```gherkin
Given 사용자가 "BMW 추천해줘"라고 입력하면
When AI 응답 아래에 인라인 캐러셀이 표시되면
Then BMW 차량만 필터링되어 3대가 표시된다 (SUV 4대가 아님)
```

## AC-5: 컴파일 통과
```gherkin
Given 모든 Provider 분리와 라우팅 변경 완료 후
When flutter analyze를 실행하면
Then 에러 0건이다
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Repository Provider 공용화 + bookmarkProvider 생성</name>
  <files>
    flutter_app/lib/core/providers/repository_providers.dart (new),
    flutter_app/lib/presentation/pages/home/providers/bookmark_provider.dart (new),
    flutter_app/lib/presentation/pages/ai_card/providers/card_provider.dart (update),
    flutter_app/lib/presentation/pages/home/providers/home_provider.dart (update),
    flutter_app/lib/presentation/pages/ai_chat/widgets/inline_card_carousel.dart (update)
  </files>
  <action>
    1. **repository_providers.dart 생성** (`core/providers/`):
       - vehicleRepositoryProvider (Provider<IVehicleRepository>) — card_provider에서 이동
       - bookmarkRepositoryProvider (Provider<IBookmarkRepository>) — 신규
       - garageRepositoryProvider (Provider<IGarageRepository>) — card_provider에서 이동
       - go_api 구현체 import (MVP 기본)
       - AppDatabase 의존성은 bookmarkRepo, garageRepo에만 필요

    2. **bookmark_provider.dart 생성** (`home/providers/`):
       - BookmarkState: Set<String> bookmarkedIds, bool isLoading
       - BookmarkNotifier extends Notifier<BookmarkState>:
         - build(): bookmarkRepo.getBookmarkedIds() 로드
         - toggleBookmark(vehicleId): 토글 + state 업데이트
         - isBookmarked(vehicleId): state에서 contains 체크
       - bookmarkProvider = NotifierProvider<BookmarkNotifier, BookmarkState>

    3. **card_provider.dart 정리**:
       - vehicleRepositoryProvider, garageRepositoryProvider 정의 제거
       - `import 'package:aicar/core/providers/repository_providers.dart'` 추가
       - CardNotifier는 ref.read(vehicleRepositoryProvider) 그대로 사용

    4. **home_provider.dart 정리**:
       - card_provider.dart import 제거
       - repository_providers.dart import 추가
       - ref.read(vehicleRepositoryProvider) 사용

    5. **inline_card_carousel.dart 정리**:
       - card_provider.dart import → repository_providers.dart import
       - vehicleRepositoryProvider, garageRepositoryProvider 사용
  </action>
  <verify>
    - `grep -r "vehicleRepositoryProvider" flutter_app/lib/core/providers/repository_providers.dart` → exists
    - `grep -r "bookmarkRepositoryProvider" flutter_app/lib/core/providers/repository_providers.dart` → exists
    - `grep "class BookmarkNotifier" flutter_app/lib/presentation/pages/home/providers/bookmark_provider.dart` → exists
    - card_provider.dart에 vehicleRepositoryProvider 정의 없음 (import만)
  </verify>
  <done>AC-1, AC-2(Provider 생성) satisfied: Repository Provider 공용화 + bookmarkProvider 생성</done>
</task>

<task type="auto">
  <name>Task 2: VehicleDetailPage 신규 + 홈 라우팅 수정</name>
  <files>
    flutter_app/lib/presentation/pages/home/vehicle_detail_page.dart (new),
    flutter_app/lib/presentation/router/app_router.dart (update),
    flutter_app/lib/presentation/router/route_names.dart (update),
    flutter_app/lib/presentation/pages/home/home_page.dart (update)
  </files>
  <action>
    1. **VehicleDetailPage 생성** (`home/vehicle_detail_page.dart`):
       - ConsumerWidget, vehicleId를 받아 vehicleRepositoryProvider.getVehicleById(id) 조회
       - 레이아웃:
         - AppBar: 뒤로가기 + "차량 상세"
         - 이미지 영역 (placeholder: Container with car icon, 높이 250)
         - 브랜드 + 모델명 (headingXl)
         - 연식 · 연료 (bodySm, textSecondary)
         - 가격 (heading2xl, secondary color)
         - 스펙 카드: 4개 항목 (마력, 토크, 연비, 제로백) — GridView 2×2
         - 하단 고정: 북마크 버튼 (BookmarkButton or IconButton)
       - 북마크 토글: ref.read(bookmarkProvider.notifier).toggleBookmark(vehicleId)
       - 북마크 상태: ref.watch(bookmarkProvider).bookmarkedIds.contains(vehicleId)
       - 디자인 토큰만 사용 (AppColors, AppSpacing, AppTypography, AppShape)

    2. **route_names.dart 업데이트**:
       - `static const String vehicleDetail = 'vehicle-detail';` 추가

    3. **app_router.dart 업데이트**:
       - /home 브랜치에 sub-route 추가:
         ```dart
         GoRoute(
           path: 'vehicle/:vehicleId',
           name: RouteNames.vehicleDetail,
           builder: (context, state) {
             final vehicleId = state.pathParameters['vehicleId']!;
             return VehicleDetailPage(vehicleId: vehicleId);
           },
         ),
         ```
       - import VehicleDetailPage

    4. **home_page.dart 라우팅 수정**:
       - `_navigateToCards` → `_navigateToVehicle`
       - `context.go('/chat/cards?q=${vehicle.brand}')` → `context.go('/home/vehicle/${vehicle.id}')`
       - 메서드명과 동작 모두 변경
  </action>
  <verify>
    - `ls flutter_app/lib/presentation/pages/home/vehicle_detail_page.dart` → exists
    - `grep "vehicleDetail" flutter_app/lib/presentation/router/route_names.dart` → exists
    - `grep "vehicle/:vehicleId" flutter_app/lib/presentation/router/app_router.dart` → exists
    - `grep "_navigateToVehicle" flutter_app/lib/presentation/pages/home/home_page.dart` → exists
    - `grep "_navigateToCards" flutter_app/lib/presentation/pages/home/home_page.dart` → 0건
  </verify>
  <done>AC-2(UI), AC-3 satisfied: VehicleDetailPage 생성 + 홈 라우팅 수정</done>
</task>

<task type="auto">
  <name>Task 3: _extractQuery 버그 수정 + flutter analyze 검증</name>
  <files>
    flutter_app/lib/presentation/pages/ai_chat/ai_chat_page.dart (update),
    flutter_app/lib/presentation/pages/ai_chat/chat_history_page.dart (update)
  </files>
  <action>
    1. **ai_chat_page.dart 수정**:
       - 문제: `_extractQuery(message.content)` — AI 응답에서 추출 → 브랜드 응답에 "SUV" 포함되어 잘못 매칭
       - 수정 방향: AI 응답 대신 해당 응답을 트리거한 **사용자 메시지**에서 쿼리 추출
       - InlineCardCarousel 빌드 시 이전 사용자 메시지 찾기:
         ```dart
         // 현재 assistant 메시지의 인덱스에서 이전 user 메시지 찾기
         String _findUserQuery(List<ChatMessage> messages, int assistantIndex) {
           for (int i = assistantIndex - 1; i >= 0; i--) {
             if (messages[i].isUser) return messages[i].content;
           }
           return '';
         }
         ```
       - `_extractQuery` 수정: 사용자 입력 텍스트 그대로 searchVehicles에 전달
         (VehicleRepositoryImpl.searchVehicles가 이미 키워드 필터 로직 보유)
       - InlineCardCarousel 호출:
         ```dart
         InlineCardCarousel(query: _findUserQuery(messages, reversedIndex))
         ```
       - `_extractQuery` 메서드 삭제 (불필요)
       - `_isRecommendationResponse`는 유지 (AI 응답 판별 용도)

    2. **chat_history_page.dart 동일 수정**:
       - 같은 패턴으로 InlineCardCarousel의 query를 사용자 메시지에서 추출

    3. **flutter analyze 실행**: 에러 0건 확인
  </action>
  <verify>
    - `grep "_extractQuery" flutter_app/lib/presentation/pages/ai_chat/ai_chat_page.dart` → 0건 (삭제됨)
    - `grep "_findUserQuery" flutter_app/lib/presentation/pages/ai_chat/ai_chat_page.dart` → exists
    - `cd flutter_app && flutter analyze` → No issues found (에러 0)
  </verify>
  <done>AC-4, AC-5 satisfied: _extractQuery 버그 수정 + 컴파일 통과</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `flutter_app/lib/domain/` — 08-01에서 확정된 엔티티/인터페이스 불변
- `flutter_app/lib/data/repositories/` — 08-01에서 생성된 구현체 불변
- `flutter_app/lib/presentation/widgets/` — 공용 위젯 불변
- `flutter_app/lib/data/datasources/` — Drift 스키마 불변

## SCOPE LIMITS
- 차고 3탭 UI (가상차고/북마크/최근본) → 08-03
- 최근 본 차량 기능 (히스토리 저장) → 08-03
- 전시장 검색/저장 기능 → 08-03
- MyPage 콘텐츠 → Phase 9

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `ls flutter_app/lib/core/providers/repository_providers.dart` → exists
- [ ] `ls flutter_app/lib/presentation/pages/home/providers/bookmark_provider.dart` → exists
- [ ] `ls flutter_app/lib/presentation/pages/home/vehicle_detail_page.dart` → exists
- [ ] `grep "vehicleDetail" flutter_app/lib/presentation/router/route_names.dart` → exists
- [ ] `grep "vehicle/:vehicleId" flutter_app/lib/presentation/router/app_router.dart` → exists
- [ ] `grep "_navigateToCards" flutter_app/lib/presentation/pages/home/home_page.dart` → 0건
- [ ] `grep "_extractQuery" flutter_app/lib/presentation/pages/ai_chat/ai_chat_page.dart` → 0건
- [ ] card_provider.dart에 vehicleRepositoryProvider 정의 없음
- [ ] `cd flutter_app && flutter analyze` → No issues found
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Repository Provider 3개 core/providers/로 공용화
- bookmarkProvider Drift 기반 영속 북마크 토글
- VehicleDetailPage 차량 상세 + 북마크 토글 표시
- 홈 차량 탭 → VehicleDetailPage (AI카드 페이지가 아님)
- _extractQuery 버그 수정 — 사용자 입력 기반 쿼리 추출
- flutter analyze 에러 0건
</success_criteria>

<output>
After completion, create `.paul/phases/08-domain-separation/08-02-SUMMARY.md`
</output>
