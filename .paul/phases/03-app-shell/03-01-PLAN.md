---
phase: 03-app-shell
plan: 01
type: execute
wave: 1
depends_on: ["02-02"]
files_modified:
  - flutter_app/lib/presentation/router/route_names.dart
  - flutter_app/lib/presentation/router/app_router.dart
  - flutter_app/lib/presentation/shell/main_shell.dart
  - flutter_app/lib/presentation/pages/home/home_page.dart
  - flutter_app/lib/presentation/pages/test_drive/test_drive_page.dart
  - flutter_app/lib/presentation/pages/ai_chat/ai_chat_page.dart
  - flutter_app/lib/presentation/pages/garage/garage_page.dart
  - flutter_app/lib/presentation/pages/my/my_page.dart
  - flutter_app/lib/main.dart
  - flutter_app/lib/app.dart
autonomous: true
---

<objective>
## Goal
GoRouter 설정 + MainShell(GNB 탭 전환) + 5개 탭 placeholder 페이지를 구현하여 앱의 네비게이션 골격을 완성한다.

## Purpose
모든 후속 화면 작업(Phase 4-8)이 이 라우트 구조 위에서 진행된다. MainShell은 GNB TabBar를 통합하고, GoRouter의 ShellRoute 패턴으로 탭 간 상태를 보존한다. app.dart의 deferred issue (appRouterProvider 미정의)를 해결한다.

## Output
- `route_names.dart` — 라우트 이름 상수
- `app_router.dart` — GoRouter config + appRouterProvider (Riverpod)
- `main_shell.dart` — Scaffold + AiCarTabBar (GNB)
- 5개 탭 placeholder 페이지 (홈, 시승, 챗봇, 차고, 마이)
- `main.dart` / `app.dart` 원복 (Widget Catalog 임시 코드 제거)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/phases/02-common-widgets/02-02-SUMMARY.md

## Source Files
@flutter_app/lib/app.dart — expects appRouterProvider
@flutter_app/lib/presentation/widgets/tab_bar/aicar_tab_bar.dart — GNB widget
@flutter_app/lib/presentation/widgets/headers/aicar_header.dart

## Architecture Reference (PLANNING.md)
- GoRouter + auth guard
- ShellRoute for GNB tabs
- 5 tabs: 홈/시승찾기/챗봇/차고/마이
- Provider 배치: appRouterProvider → core/providers/ (2+ feature 공유)
- Riverpod for state management
</context>

<acceptance_criteria>

## AC-1: GoRouter provides working tab navigation
```gherkin
Given the app launches with GoRouter
When navigating to /home, /test-drive, /chat, /garage, /my
Then each route renders its respective placeholder page inside MainShell
And the GNB TabBar reflects the active tab
And browser back/forward works correctly
```

## AC-2: MainShell preserves tab state with StatefulShellRoute
```gherkin
Given the user is on the chat tab and scrolls down
When switching to garage tab and back to chat tab
Then the chat tab scroll position is preserved
And GNB TabBar active index matches the visible tab
```

## AC-3: app.dart and main.dart are fully functional
```gherkin
Given main.dart restores Riverpod/KakaoSdk/dotenv setup
When running flutter run
Then the app starts with GoRouter navigation
And the GNB shows 5 tabs with Figma SVG icons
And no import errors exist
```

## AC-4: All files compile cleanly
```gherkin
Given all router, shell, and page files exist
When running dart analyze
Then zero errors reported
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create route names and GoRouter configuration</name>
  <files>
    flutter_app/lib/presentation/router/route_names.dart,
    flutter_app/lib/presentation/router/app_router.dart
  </files>
  <action>
    **route_names.dart:**
    Abstract final class `RouteNames` with static const String fields:
    - home = 'home', testDrive = 'test-drive', chat = 'chat'
    - garage = 'garage', my = 'my'
    - splash = 'splash', login = 'login', consent = 'consent' (Phase 4 용 예약)

    **app_router.dart:**
    - Import flutter_riverpod, go_router
    - Create `appRouterProvider` as a Provider<GoRouter>
    - Use `StatefulShellRoute.indexedStack` for tab preservation:
      - 5 branches, one per tab
      - Each branch has a single GoRoute with its placeholder page
      - navigatorKey per branch for independent navigation stacks
    - `StatefulShellRoute` builder returns `MainShell` widget
    - Initial location: '/home'
    - Route paths: /home, /test-drive, /chat, /garage, /my
    - Auth redirect 로직은 Phase 4에서 추가 — 지금은 placeholder comment만

    GoRouter는 Riverpod Provider로 제공 (app.dart에서 ref.watch(appRouterProvider)로 사용 중)
  </action>
  <verify>dart analyze lib/presentation/router/ — zero errors</verify>
  <done>AC-1 partial: Routes defined, AC-4 partial</done>
</task>

<task type="auto">
  <name>Task 2: Create MainShell with GNB integration</name>
  <files>flutter_app/lib/presentation/shell/main_shell.dart</files>
  <action>
    Create `MainShell` StatelessWidget:

    ```dart
    MainShell({
      required StatefulNavigationShell navigationShell,
    })
    ```

    **Implementation:**
    - Scaffold with:
      - body: navigationShell (GoRouter provides this)
      - bottomNavigationBar: AiCarTabBar(
          currentIndex: navigationShell.currentIndex,
          onTap: (index) => navigationShell.goBranch(index),
        )
    - No AppBar (individual pages handle their own headers)
    - backgroundColor: AppColors.background

    This widget is used as the builder for StatefulShellRoute.
  </action>
  <verify>dart analyze lib/presentation/shell/main_shell.dart — zero errors</verify>
  <done>AC-2 satisfied: MainShell uses StatefulShellRoute for tab state preservation</done>
</task>

<task type="auto">
  <name>Task 3: Create placeholder pages and restore main.dart/app.dart</name>
  <files>
    flutter_app/lib/presentation/pages/home/home_page.dart,
    flutter_app/lib/presentation/pages/test_drive/test_drive_page.dart,
    flutter_app/lib/presentation/pages/ai_chat/ai_chat_page.dart,
    flutter_app/lib/presentation/pages/garage/garage_page.dart,
    flutter_app/lib/presentation/pages/my/my_page.dart,
    flutter_app/lib/main.dart,
    flutter_app/lib/app.dart
  </files>
  <action>
    **5 placeholder pages:**
    Each is a simple StatelessWidget with:
    - Scaffold body: Center(child: Column [Icon, SizedBox, Text "탭이름"])
    - Use AppTypography.heading2xl for title, AppColors.textSecondary for color
    - No AppBar (MainShell handles navigation)
    - Each file: ~20 lines

    Pages: HomePage, TestDrivePage, AiChatPage, GaragePage, MyPage

    Note: ai_chat_page.dart already exists but is empty — overwrite with placeholder.

    **Restore main.dart:**
    Restore to original from main branch (dotenv, KakaoSdk, ProviderScope + AiCarApp)
    — remove Widget Catalog temporary code

    **app.dart stays as-is** — already imports appRouterProvider correctly
  </action>
  <verify>cd flutter_app && dart analyze lib/presentation/pages/home/ lib/presentation/pages/test_drive/ lib/presentation/pages/ai_chat/ lib/presentation/pages/garage/ lib/presentation/pages/my/ lib/presentation/router/ lib/presentation/shell/ lib/app.dart lib/main.dart 2>&1</verify>
  <done>AC-3 satisfied: main.dart restored, app functional. AC-1 complete: all tabs render. AC-4 satisfied: all files clean.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- flutter_app/lib/core/theme/* (Phase 1 — locked)
- flutter_app/lib/presentation/widgets/* (Phase 2 — locked, except TabBar already updated)
- flutter_app/lib/presentation/pages/_dev/* (Widget Catalog — keep as-is)

## SCOPE LIMITS
- Do NOT implement auth guard logic (Phase 4)
- Do NOT implement actual page content (Phase 5-8)
- Do NOT add splash/login/consent routes yet (Phase 4)
- Placeholder pages only — no business logic, no providers, no API calls

</boundaries>

<verification>
- [ ] GoRouter navigates between all 5 tabs
- [ ] GNB TabBar active index syncs with current route
- [ ] Tab state preserved when switching (StatefulShellRoute)
- [ ] main.dart restored with Riverpod/KakaoSdk/dotenv
- [ ] app.dart's appRouterProvider resolves (deferred issue closed)
- [ ] `dart analyze` zero errors on all new files
</verification>

<success_criteria>
- All 3 tasks completed
- All 4 acceptance criteria met
- Deferred issue resolved (appRouterProvider)
</success_criteria>

<output>
After completion, create `.paul/phases/03-app-shell/03-01-SUMMARY.md`
</output>
