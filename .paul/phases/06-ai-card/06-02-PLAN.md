---
phase: 06-ai-card
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - flutter_app/lib/presentation/pages/ai_card/card_back_widget.dart
  - flutter_app/lib/presentation/pages/ai_card/widgets/radar_chart.dart
  - flutter_app/lib/presentation/pages/ai_card/card_customize_page.dart
  - flutter_app/lib/presentation/pages/ai_card/card_front_widget.dart
  - flutter_app/lib/presentation/pages/ai_chat/widgets/inline_card_carousel.dart
  - flutter_app/lib/presentation/router/app_router.dart
  - flutter_app/lib/presentation/router/route_names.dart
autonomous: true
---

<objective>
## Goal
카드 뒷면(상세 스펙 + Radar chart)과 카드 커스터마이즈 페이지를 구현하여 차량 비교 경험을 완성한다.

## Purpose
카드 앞면만으로는 차량 간 비교가 어렵다. 뒷면의 Radar chart로 성능을 시각적으로 비교하고, 커스터마이즈 페이지에서 옵션을 조정할 수 있게 한다.

## Output
- CardBackWidget (상세 스펙 + Radar chart)
- RadarChart 커스텀 위젯 (CustomPainter)
- CardCustomizePage (옵션 선택 UI)
- 카드 탭 → flip 애니메이션 (앞↔뒤)
</objective>

<context>
## Project Context
@.paul/phases/06-ai-card/06-01-SUMMARY.md

## Source Files
@flutter_app/lib/presentation/pages/ai_card/card_front_widget.dart
@flutter_app/lib/presentation/pages/ai_card/providers/card_provider.dart
@flutter_app/lib/domain/entities/vehicle_card.dart
</context>

<acceptance_criteria>

## AC-1: 카드 뒷면 + Radar Chart
```gherkin
Given 카드 앞면이 표시된 상태에서
When 카드를 탭하면
Then 카드가 flip 애니메이션으로 뒤집히고,
     뒷면에 상세 스펙(마력, 토크, 연비, 제로백)과
     Radar chart가 표시된다
```

## AC-2: 카드 커스터마이즈
```gherkin
Given 카드 뒷면에서 "커스터마이즈" 버튼을 탭하면
When CardCustomizePage가 표시되면
Then 옵션 선택(색상, 트림) UI가 표시되고
     선택 결과가 카드에 반영된다 (MVP: UI만, 실제 데이터 변경 없음)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: RadarChart 커스텀 위젯</name>
  <files>
    flutter_app/lib/presentation/pages/ai_card/widgets/radar_chart.dart
  </files>
  <action>
    **RadarChart** (`widgets/radar_chart.dart`):
    - CustomPainter 기반 5각형 레이더 차트
    - 축: 마력, 토크, 연비, 제로백, 가격(역수)
    - VehicleSpecs + price를 받아 0~1 정규화
    - 정규화 기준: 마력 0~400, 토크 0~60, 연비 0~25, 제로백 12~3(역수), 가격 10000~3000(역수)
    - 배경: 5단계 그리드 라인 (AppColors.textDisabled, 0.3 opacity)
    - 데이터: AppColors.secondary fill (0.3 opacity) + stroke (2px)
    - 축 라벨: AppTypography.captionXs, AppColors.textTertiary
    - 크기: 200×200px
    - const constructor 불가 (CustomPainter)

    Avoid: 외부 차트 라이브러리 사용하지 않음 (CustomPainter로 직접 구현)
  </action>
  <verify>flutter analyze 에러 0건</verify>
  <done>AC-1 부분 충족: Radar chart 위젯 준비.</done>
</task>

<task type="auto">
  <name>Task 2: CardBackWidget + flip 애니메이션</name>
  <files>
    flutter_app/lib/presentation/pages/ai_card/card_back_widget.dart,
    flutter_app/lib/presentation/pages/ai_chat/widgets/inline_card_carousel.dart
  </files>
  <action>
    **CardBackWidget** (`card_back_widget.dart`):
    - VehicleCard를 받아 뒷면 렌더링
    - 크기: CardFrontWidget과 동일 (296×280px 풀사이즈 / 200×170px 컴팩트)
    - `isCompact` bool 파라미터로 크기 분기
    - 배경: AppColors.cardBackground (slate-500)
    - 상단: RadarChart (컴팩트 시 120×120px)
    - 하단: 스펙 리스트 (마력, 토크, 연비, 제로백) — 아이콘 + 라벨 + 값
    - "커스터마이즈" 텍스트 버튼 (하단, AppColors.secondary)

    **InlineCardCarousel에 flip 적용**:
    - _CompactCard를 StatefulWidget으로 변환
    - GestureDetector onTap → AnimatedSwitcher 또는 Transform.flip
    - 앞면(CardFront 컴팩트) ↔ 뒷면(CardBack 컴팩트) 전환
    - AnimationController 500ms, Curves.easeInOut

    Avoid: 새 디자인 토큰 추가하지 않음
  </action>
  <verify>flutter analyze 에러 0건</verify>
  <done>AC-1 완전 충족: 카드 탭 → flip → 뒷면 스펙 + Radar chart.</done>
</task>

<task type="auto">
  <name>Task 3: CardCustomizePage + 라우팅</name>
  <files>
    flutter_app/lib/presentation/pages/ai_card/card_customize_page.dart,
    flutter_app/lib/presentation/router/app_router.dart,
    flutter_app/lib/presentation/router/route_names.dart
  </files>
  <action>
    **RouteNames 추가**: `static const String cardCustomize = 'card-customize';`

    **GoRouter /chat/cards/customize 서브라우트**

    **CardCustomizePage** (`card_customize_page.dart`):
    - ConsumerWidget
    - AiCarHeader(title: '커스터마이즈', showBack: true)
    - 색상 선택: 6개 컬러 칩 (흰색, 검정, 실버, 파랑, 빨강, 회색)
    - 트림 선택: AiCarChip 3개 (기본, 프리미엄, M스포츠/AMG)
    - 하단: 선택된 카드 미리보기 (CardFrontWidget)
    - MVP: UI만 구현, 실제 데이터 변경 없음 (선택 상태만 로컬)

    **CardBackWidget 커스터마이즈 버튼 연결**:
    - onTap → context.pushNamed(RouteNames.cardCustomize)

    Avoid: 실제 옵션 데이터 API 없음 (하드코딩 옵션 목록)
  </action>
  <verify>flutter analyze 에러 0건</verify>
  <done>AC-2 충족: 커스터마이즈 페이지 UI + 네비게이션.</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- `flutter_app/lib/core/theme/*` — 디자인 토큰 수정 금지
- `flutter_app/lib/presentation/widgets/*` — 기존 공용 위젯 수정 금지
- `flutter_app/lib/domain/entities/vehicle_card.dart` — 엔티티 수정 금지

## SCOPE LIMITS
- 실제 옵션 데이터 API 없음 (MVP 하드코딩)
- 커스터마이즈 결과 저장 없음 (UI 상태만)
</boundaries>

<verification>
- [ ] `flutter analyze` 새 코드 에러 0건
- [ ] Radar chart 렌더링 정상
- [ ] 카드 flip 애니메이션 동작
- [ ] 커스터마이즈 페이지 네비게이션 + UI
</verification>

<success_criteria>
- 3개 태스크 완료, flutter analyze 통과
- Radar chart 5축 정상 표시
- 카드 flip 애니메이션 자연스러움
- Phase 6 완료
</success_criteria>

<output>
After completion, create `.paul/phases/06-ai-card/06-02-SUMMARY.md`
</output>
