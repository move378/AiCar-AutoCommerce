---
phase: 02-common-widgets
plan: 03
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - flutter_app/lib/presentation/widgets/cards/vehicle_card.dart
  - flutter_app/lib/presentation/widgets/buttons/bookmark_button.dart
  - flutter_app/lib/presentation/widgets/map/map_pin.dart
autonomous: true
---

<objective>
## Goal
Create three content widgets — VehicleCard (List/Card variants), BookmarkButton, MapPin — for vehicle display across screens.

## Output
- `presentation/widgets/cards/vehicle_card.dart` — List(343×210) and Card(200×190) variants
- `presentation/widgets/buttons/bookmark_button.dart` — Toggle bookmark icon
- `presentation/widgets/map/map_pin.dart` — Brand-labeled map marker
</objective>

<context>
## Figma Analysis
VehicleCard List — node 2450:3665: 343×210px, image(140px)+info(62px), 2 sections
VehicleCard Card — node 2450:3684: 200×190px, image(120px)+info(62px), compact
MapPin — node 2534:2574: 50×62px, shadow+pin(#1E293B)+brand circle(42px)
</context>

<acceptance_criteria>

## AC-1: VehicleCard renders both List and Card variants
```gherkin
Given a VehicleCard widget with variant parameter
When variant is list: width fills parent, height ~210, image 140px
When variant is card: width 200, height ~190, image 120px
Then both show: image placeholder, vehicle name, price, bookmark
And border radius is AppShape.radiusMd
And elevation uses AppElevation.elevation1
```

## AC-2: BookmarkButton toggles between saved/unsaved
```gherkin
Given a BookmarkButton with isBookmarked and onTap
When isBookmarked is true: filled icon in AppColors.secondary
When isBookmarked is false: outlined icon in AppColors.textTertiary
And onTap callback fires when tapped
```

## AC-3: MapPin renders brand marker
```gherkin
Given a MapPin with brandName, brandLogoUrl, isSelected
When isSelected is true: pin color AppColors.primary, larger shadow
When isSelected is false: pin color AppColors.textTertiary, subtle shadow
And brand circle (42px) shows brand initial or image
```

## AC-4: All widgets compile cleanly
```gherkin
Given all widget files exist
When running dart analyze
Then zero errors reported
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create VehicleCard widget</name>
  <files>flutter_app/lib/presentation/widgets/cards/vehicle_card.dart</files>
  <action>
    Create `VehicleCard` with:

    ```dart
    enum VehicleCardVariant { list, card }

    VehicleCard({
      required String name,
      required String price,
      String? imageUrl,
      String? subtitle,
      bool isBookmarked = false,
      VoidCallback? onTap,
      VoidCallback? onBookmarkTap,
      VehicleCardVariant variant = VehicleCardVariant.list,
    })
    ```

    **List variant (343×210):**
    - Column: [Image container 140px] [Info area 62px + padding]
    - Image container: ClipRRect(radiusMd top only), grey placeholder with car icon if no imageUrl
    - Info: Row [Column(name in bodySm/w600, subtitle in captionXs/textSecondary, price in bodyMd/w600)] [BookmarkButton]
    - Full parent width

    **Card variant (200×190):**
    - Same structure, width 200, image height 120px
    - Compact info layout

    **Common:**
    - Container with AppShape.radiusMd, AppElevation.elevation1, white bg
    - GestureDetector for onTap
    - Padding: AppSpacing.space3 for info area
  </action>
  <verify>dart analyze lib/presentation/widgets/cards/vehicle_card.dart</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Create BookmarkButton widget</name>
  <files>flutter_app/lib/presentation/widgets/buttons/bookmark_button.dart</files>
  <action>
    Create `BookmarkButton` with:

    ```dart
    BookmarkButton({
      required bool isBookmarked,
      required VoidCallback? onTap,
      double size = 24,
    })
    ```

    **Implementation:**
    - GestureDetector wrapping Icon
    - Bookmarked: Icons.bookmark, AppColors.secondary
    - Not bookmarked: Icons.bookmark_border, AppColors.textTertiary
    - Padding: AppSpacing.space1 hit area expansion
  </action>
  <verify>dart analyze lib/presentation/widgets/buttons/bookmark_button.dart</verify>
  <done>AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Create MapPin widget</name>
  <files>flutter_app/lib/presentation/widgets/map/map_pin.dart</files>
  <action>
    Create `MapPin` with:

    ```dart
    MapPin({
      required String brandName,
      bool isSelected = false,
      String? brandLogoUrl,
    })
    ```

    **Implementation (Figma: 50×62px):**
    - CustomPaint or Stack approach
    - Stack: positioned shadow ellipse at bottom, pin body (Container with pointed bottom via ClipPath or decoration), brand circle (42px) centered
    - Selected: pin AppColors.primary, larger shadow
    - Unselected: pin AppColors.textTertiary, subtle shadow
    - Brand circle: 42px CircleAvatar with brandName initial, white bg
    - Total size: SizedBox(width: 50, height: 62)
    - Use Stack with positioned children to layer shadow, pin, logo
  </action>
  <verify>dart analyze lib/presentation/widgets/map/map_pin.dart</verify>
  <done>AC-3 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- flutter_app/lib/core/theme/*
- flutter_app/lib/presentation/widgets/buttons/aicar_button.dart
- flutter_app/lib/presentation/widgets/inputs/*
- flutter_app/lib/presentation/widgets/chips/*
- flutter_app/lib/presentation/widgets/tab_bar/*
- flutter_app/lib/presentation/widgets/headers/*

## SCOPE LIMITS
- No network image loading (use placeholder containers)
- No state management providers
- No page-level integration

</boundaries>

<verification>
- [ ] All 3 widget files exist
- [ ] `dart analyze lib/presentation/widgets/` zero errors
- [ ] VehicleCard has both variant enum values
- [ ] BookmarkButton toggles icon/color
- [ ] MapPin renders with brand initial
- [ ] No hardcoded colors
</verification>

<success_criteria>
- All tasks completed, all ACs met, dart analyze clean
</success_criteria>

<output>
After completion, create `.paul/phases/02-common-widgets/02-03-SUMMARY.md`
</output>
