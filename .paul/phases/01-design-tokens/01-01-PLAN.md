---
phase: 01-design-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flutter_app/pubspec.yaml
  - flutter_app/lib/main.dart
  - flutter_app/lib/app.dart
  - flutter_app/lib/core/theme/app_colors.dart
  - flutter_app/lib/core/theme/app_typography.dart
  - flutter_app/lib/core/theme/app_spacing.dart
  - flutter_app/lib/core/theme/app_shape.dart
  - flutter_app/lib/core/theme/app_elevation.dart
  - flutter_app/lib/core/theme/app_theme.dart
autonomous: true
---

<objective>
## Goal
Create all design token files in `lib/core/theme/` and integrate them into `AppTheme.light` ThemeData — establishing the visual foundation for the entire Flutter app.

## Purpose
PLANNING.md section 5 defines the complete design system (Tailwind v4 Slate+Emerald, Pretendard, 4px spacing). All subsequent widgets and screens depend on these tokens. This is the first step of the `feat/flutter/design-system` branch workflow.

## Output
- `lib/core/theme/app_colors.dart` — semantic color tokens
- `lib/core/theme/app_typography.dart` — type scale with Pretendard
- `lib/core/theme/app_spacing.dart` — 4px-based spacing constants
- `lib/core/theme/app_shape.dart` — border radius + padding presets
- `lib/core/theme/app_elevation.dart` — box shadow definitions (3 levels)
- `lib/core/theme/app_theme.dart` — ThemeData.light composing all tokens
- Restored foundation files: `pubspec.yaml`, `main.dart`, `app.dart`
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Source References
- PLANNING.md section 5-3: Color tokens (semantic, background, text, state, GNB, component)
- PLANNING.md section 5-4: Typography tokens (Pretendard, 9-step scale, -2% letter spacing)
- PLANNING.md section 5-5: Shape tokens (border radius, padding)
- PLANNING.md section 5-6: Spacing (4px multiples: space-1 through space-10)
- PLANNING.md section 5-7: Elevation (3 levels, light mode only)

## Current State
- Branch: `feat/flutter/design-system` at main HEAD
- All Flutter files in working tree are empty (clean-slate rebuild)
- main branch has working `pubspec.yaml`, `main.dart`, `app.dart`
- `app.dart` on main already imports `AppTheme.light` from `core/theme/app_theme.dart`
- Pretendard fonts registered in pubspec.yaml (Regular/Medium/SemiBold/Bold)
</context>

<acceptance_criteria>

## AC-1: Color tokens are complete and typed
```gherkin
Given the AppColors class exists in core/theme/app_colors.dart
When referencing any semantic role from PLANNING.md section 5-3
Then a static const Color field exists with the correct hex value
And all categories are covered: semantic, background, text, state, GNB, component
```

## AC-2: Typography tokens match the type scale
```gherkin
Given the AppTypography class exists in core/theme/app_typography.dart
When referencing any style from PLANNING.md section 5-4 (4xl through 2xs)
Then a static TextStyle getter exists with correct size, lineHeight, weight
And fontFamily is 'Pretendard'
And letterSpacing is -2% of fontSize
```

## AC-3: Spacing, shape, and elevation tokens are defined
```gherkin
Given AppSpacing, AppShape, and AppElevation classes exist
When referencing any token from PLANNING.md sections 5-5, 5-6, 5-7
Then correct constant values exist (spacing as double, radius as BorderRadius, elevation as BoxShadow list)
```

## AC-4: AppTheme.light compiles and integrates all tokens
```gherkin
Given app.dart uses AppTheme.light as the MaterialApp theme
When running flutter analyze on the project
Then zero errors are reported
And ThemeData uses the correct colorScheme, textTheme, and component themes from tokens
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Restore foundation files from main</name>
  <files>flutter_app/pubspec.yaml, flutter_app/lib/main.dart, flutter_app/lib/app.dart</files>
  <action>
    Restore these three files from main branch content (git show main:path):
    - pubspec.yaml: Full content with all dependencies, Pretendard font registration, assets
    - main.dart: WidgetsFlutterBinding, dotenv, KakaoSdk init, ProviderScope + AiCarApp
    - app.dart: ConsumerWidget using AppTheme.light, MaterialApp.router with GoRouter

    These files are prerequisites for compilation. Keep them identical to main — no modifications needed.

    Note: app.dart imports `core/theme/app_theme.dart` which will be created in Task 3.
  </action>
  <verify>Files exist and have non-zero content: wc -c flutter_app/pubspec.yaml flutter_app/lib/main.dart flutter_app/lib/app.dart</verify>
  <done>AC-4 partial: Foundation files restored, ready for theme integration</done>
</task>

<task type="auto">
  <name>Task 2: Create design token files (colors, typography, spacing, shape, elevation)</name>
  <files>
    flutter_app/lib/core/theme/app_colors.dart,
    flutter_app/lib/core/theme/app_typography.dart,
    flutter_app/lib/core/theme/app_spacing.dart,
    flutter_app/lib/core/theme/app_shape.dart,
    flutter_app/lib/core/theme/app_elevation.dart
  </files>
  <action>
    Create `lib/core/theme/` directory and five token files:

    **app_colors.dart** — Abstract final class with static const Color fields:
    - Semantic: primary (#1E293B slate-800), secondary (#10B981 emerald-500), secondaryHover (#059669 emerald-600)
    - Background: background (#FFFFFF), surface (#F8FAFC slate-50), cardBackground (#64748B slate-500)
    - Text: textPrimary (#0F172A slate-900), textSecondary (#64748B slate-500), textTertiary (#94A3B8 slate-400), textDisabled (#CBD5E1 slate-300), textOnDark (#FFFFFF), textAccent (#059669 emerald-600)
    - State: success (#10B981), error (#EF4444), warning (#F59E0B), info (#3B82F6)
    - GNB: gnbBackground (#FFFFFF), gnbActive (#0F172A slate-900), gnbInactive (#94A3B8 slate-400)
    - Component: buttonSolidDefault (#1E293B), buttonSolidDisabled (#F1F5F9 slate-100), buttonOutlineDefault (#FFFFFF), chipSelected (#334155 slate-700), chipUnselected (#FFFFFF)

    **app_typography.dart** — Abstract final class with static TextStyle getters:
    - Font: Pretendard, letterSpacing = fontSize * -0.02
    - Scale: display4xl (36/44/700), display3xl (30/38/700), heading2xl (24/32/600), headingXl (20/30/600), bodyLg (18/28/400), bodyMd (16/24/400), bodySm (14/20/400), captionXs (12/16/400), overline2xs (11/14/500)
    - Import flutter/material.dart only

    **app_spacing.dart** — Abstract final class with static const double:
    - space1=4, space2=8, space3=12, space4=16, space5=20, space6=24, space8=32, space10=40

    **app_shape.dart** — Abstract final class with:
    - Static const BorderRadius: radiusMd (10px for button/card/input), radiusFull (100px for chip/pill), radiusCircle (999px for avatar)
    - Static const EdgeInsets for common padding: buttonPadding (h16,v10), chipPadding (h10,v6), tabBarPadding (h16,v0)

    **app_elevation.dart** — Abstract final class with static List<BoxShadow> getters:
    - elevation0: none (empty list)
    - elevation1: [BoxShadow(offset: Offset(0,1), blurRadius: 3, color: Color(0x140F172A))] — card, dropdown
    - elevation2: [BoxShadow(offset: Offset(0,4), blurRadius: 12, color: Color(0x1F0F172A))] — modal, FAB
    - elevation3: [BoxShadow(offset: Offset(0,8), blurRadius: 24, color: Color(0x290F172A))] — bottomsheet, overlay

    Use `abstract final class` pattern (Dart 3) — no instantiation, pure namespace.
    Import only `package:flutter/material.dart` where needed.
    All values from PLANNING.md section 5 — do not invent additional tokens.
  </action>
  <verify>All 5 files exist in lib/core/theme/ and each contains the expected class name: grep -l "abstract final class App" flutter_app/lib/core/theme/*.dart</verify>
  <done>AC-1 satisfied: all color tokens present. AC-2 satisfied: all typography tokens present. AC-3 satisfied: spacing, shape, elevation tokens present.</done>
</task>

<task type="auto">
  <name>Task 3: Create AppTheme composing all tokens into ThemeData</name>
  <files>flutter_app/lib/core/theme/app_theme.dart</files>
  <action>
    Create `lib/core/theme/app_theme.dart`:

    Abstract final class AppTheme with a static ThemeData get `light`:
    - colorScheme: ColorScheme.light() with:
      - primary: AppColors.primary
      - secondary: AppColors.secondary
      - surface: AppColors.surface
      - error: AppColors.error
      - onPrimary: AppColors.textOnDark
      - onSecondary: AppColors.textOnDark
      - onSurface: AppColors.textPrimary
      - onError: AppColors.textOnDark
    - scaffoldBackgroundColor: AppColors.background
    - textTheme: TextTheme() mapping AppTypography styles to Material slots:
      - displayLarge: display4xl
      - displayMedium: display3xl
      - headlineLarge: heading2xl
      - headlineMedium: headingXl
      - bodyLarge: bodyLg
      - bodyMedium: bodyMd
      - bodySmall: bodySm
      - labelLarge: captionXs
      - labelSmall: overline2xs
    - appBarTheme: transparent background, no elevation, textPrimary foreground
    - elevatedButtonTheme / outlinedButtonTheme: using AppColors.buttonSolid*, AppShape.radiusMd
    - chipTheme: using AppColors.chipSelected, AppShape.radiusFull
    - bottomNavigationBarTheme: gnbBackground, gnbActive, gnbInactive
    - useMaterial3: true

    Import all token files from the same directory.
    This file is already imported by app.dart as `AppTheme.light`.
  </action>
  <verify>Run `cd flutter_app && flutter analyze --no-pub 2>&1 | head -20` — expect zero errors related to theme files. If flutter not available, verify with `dart analyze lib/core/theme/`.</verify>
  <done>AC-4 satisfied: AppTheme.light compiles, integrates all tokens, and is consumed by app.dart</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- backend/* (backend team's domain)
- flutter_app/lib/domain/* (interfaces — will be rebuilt in later phases)
- flutter_app/lib/data/* (repositories — will be rebuilt in later phases)

## SCOPE LIMITS
- This plan creates ONLY design token files in core/theme/ + foundation files
- Do NOT create any widgets (Phase 2)
- Do NOT create any pages or screens
- Do NOT add new dependencies to pubspec.yaml beyond what main already has
- Do NOT create dark mode theme (PLANNING.md specifies light mode only)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] All 6 theme files exist in flutter_app/lib/core/theme/
- [ ] pubspec.yaml, main.dart, app.dart restored with non-zero content
- [ ] Color token count matches PLANNING.md section 5-3 (all categories)
- [ ] Typography scale has all 9 levels (4xl through 2xs)
- [ ] Spacing has all 8 values (space1 through space10)
- [ ] `flutter analyze` or `dart analyze` passes with zero errors on theme files
- [ ] No files outside core/theme/ were modified (except foundation restores)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- All 4 acceptance criteria met
- Design tokens exactly match PLANNING.md section 5 specifications
- AppTheme.light is a valid ThemeData consumed by app.dart
</success_criteria>

<output>
After completion, create `.paul/phases/01-design-tokens/01-01-SUMMARY.md`
</output>
